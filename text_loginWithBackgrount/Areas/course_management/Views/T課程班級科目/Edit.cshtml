@model Class_system_Backstage_pj.Models.T課程班級科目

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>T課程班級科目</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="班級科目id" />
           
            <div class="form-group" >
                <label asp-for="科目id" class="control-label"></label>
                <input asp-for="科目id" class="form-control" value="科目id"  hidden/>
                <span>@ViewData["科目名稱"]</span>
            </div>

            <div class="form-group" hidden>
                <label asp-for="班級id" class="control-label"></label>
                <input asp-for="班級id" class="form-control" value="班級id"/>
            </div>

            <div class="form-group">
                <label asp-for="老師id" class="control-label"></label>
                <select asp-for="老師id" class="form-control" asp-items="ViewBag.老師id"></select>
                <span asp-validation-for="老師id" class="text-danger"></span>
            </div>

            <div class="form-group" hidden>
                <label asp-for="狀態" class="control-label"></label>
                <input asp-for="狀態" class="form-control" value="1"/>
            </div>

            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
